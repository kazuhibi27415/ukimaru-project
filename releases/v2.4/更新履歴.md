# PavlokSuperChat 更新履歴

## 2.4 — 2026年9月14日

- 起動時の履歴を既読扱いにした際、既読にしたSuper Chatの件数を画面とログへ表示するようにしました。
- 通常チャットやSuper Stickerは、既読Super Chat件数に含まれません。
- 初回履歴全体の既読登録、起動前イベントのZap判定除外など、従来の動作は維持しています。

## 2.3 — 2026年9月14日

- 選択したOAuthクライアントJSONを利用者のアプリ管理フォルダへコピーするようにしました。
- 2.0～2.2で指定した元JSONが残っている場合、初回起動時に管理場所へ自動移行します。
- 元JSONを削除済みでも、暗号化された保存済みGoogleログインから復元可能な場合は自動修復します。
- コピー先をconfig.iniへ保存するため、旧バージョンの配布フォルダを削除しても影響しません。
- OAuthアクセストークンや更新トークンをJSONの管理コピーへ含めないことを確認しています。
- Super Chatの対象条件、FIFO、クールダウン、Pavlok送信失敗時の再送禁止は変更していません。

## 2.2 — 2026年9月14日

- Pavlok初期トークンをWindows DPAPIで暗号化し、config.iniへ平文保存しないようにしました。
- 1.0～2.1の平文トークンは初回起動時に自動移行し、その他のINI設定を維持します。
- GUI監視プロセスのエラー時に閉じた標準入力を待たず、本来のエラーだけをログへ残して終了します。
- Super Chatの対象条件、FIFO、クールダウン、Pavlok送信失敗時の再送禁止は変更していません。

## 2.1 — 2026年9月14日

- 起動時に公開リリースを確認し、新しい正式版がある場合だけ画面上部に小さく通知します。
- 通知からGitHub Releasesを開けます。自動更新は行いません。
- GUIの監視ログをUTF-8で保存し、画面に「ログフォルダ」ボタンを追加しました。
- ログは7日以内・直近10回、1回あたり末尾約2MBを保持し、古いものを自動削除します。
- 認証文字列を伏せて保存し、保存失敗時も監視を続行します。
- 既存INIの引き継ぎ、Super Chatの対象条件、FIFO、Pavlok再送禁止は維持しています。

## 2.0 — 2026年9月13日

- メン限向けに配信者のGoogleログイン（OAuth）方式を追加。従来のAPIキー方式も選択できます。
- GUIに認証方式・OAuthクライアントJSONの選択・保存済みログインの解除を追加しました。
- OAuthで動画情報とgRPCチャットを取得し、認証期限切れ時はトークンを更新します。
- ログイン情報はWindows DPAPIで暗号化して利用者フォルダへ保存します。
- Super Chatのみ・JPY・金額完全一致・FIFO・Pavlok再送禁止などの既存ルールは維持しています。
- メン限の実配信での動作確認は未実施です。YouTube API側の権限制限で取得できない場合があります。

## 1.1 — 2026年9月13日

- GUI入力欄に右クリックの「コピー」「貼り付け」「切り取り」「すべて選択」を追加しました。
- Ctrl+C／V／X／Aとログ欄のコピーに対応しました。監視中の編集禁止は維持しています。
- 配布先を `releases/v1.1/` に変更し、過去の1.0も `releases/v1.0/` に保存します。
- EXE・README・更新履歴をGitで管理します。

## 1.0 — 2026年9月13日

これまでの開発版の機能・修正をまとめ、現在のバージョンを1.0としました。

### 主な機能

- YouTubeライブのSuper ChatをgRPCで監視。日本円の指定金額と完全一致したイベントを対象にします。
- 最大4つの金額グループに、それぞれ固定出力・ランダム出力を設定できます。不要なグループは無効化できます。
- GUIで認証情報、対象金額、出力、待ち時間を編集し、監視を開始・停止できます。
- GUIの出力方法を「固定」「ランダム」と表示。通常起動時にコマンド画面は表示しません。
- 全グループを共通のFIFO予約キューで処理し、delayとcooldownを適用します。
- 実機へ送らずに動作を確認できるDRY-RUNに対応しています。

### 配布・設定保存

- EXEに初期設定を同梱し、EXE単体で配布できるようにしました。Pythonのインストールは不要です。
- 初回起動時に `%LOCALAPPDATA%\PavlokSuperChat\config.ini` を作成します。
- 保存済みの利用者設定は、再起動やEXE更新時に上書きしません。
- GUIのタイトルと見出し、コンソール起動時にバージョンを表示します。
- READMEに設定方法、監視の開始・停止、旧設定の移行方法を記載しました。

### 修正内容

- 待ち時間に `nan`・`inf` を指定した場合、起動時に拒否するようにしました。
- HTTPリダイレクトによるZapの自動再送を禁止し、成功判定を2xxに限定しました。
- 不正な認証応答での例外や空トークンの保存を防止しました。
- GUIと監視プロセス間のログをUTF-8に統一し、日本語の文字化けと円記号による停止を修正しました。
- 設定ファイルと説明書の日本語をUTF-8 BOM付きで保存するようにしました。

### 維持している動作

- Super Sticker・日本円以外は対象外、Pavlok刺激はZap固定です。
- 送信失敗時に同じZapを自動再送しません。401/403では次の予約用のトークンだけ更新します。
- 停止・終了時の未送信予約は破棄し、次回に引き継ぎません。送信済みZapは取り消せません。
- `--console`で従来のコンソール操作も使用できます。

※ 開発版で確認済みの実機動作と、模擬データによる回帰テストは区別して扱っています。
1.0の全機能を組み合わせた実機送信試験は未実施です。
